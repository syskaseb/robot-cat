// Grzbiet, tyl. Top skin from the midpoint back, carrying the rump and the
// tail root. Same print orientation as the front half.
include <shell_lib.scad>

module shell_back_rear() { body_panel(top = true, fore = false); }

shell_back_rear();
